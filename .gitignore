node_modules/
dist/
src-tauri/target/
src-tauri/gen/
portable/
*.log
.DS_Store
crates/*/target/
__pycache__/
*.pyc

# stray model weights downloaded by ultralytics/rembg into CWD
*.pt
weights/

# updater signing key — must NEVER be committed; lives at %USERPROFILE%\.cozyverse-updater.key,
# outside this repo, but this guards against it ever being copied in by habit. See docs/auto-updater.md.
*.key
!*.key.pub
